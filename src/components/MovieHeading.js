import React from  'react'

const MovieListHeading = ({headings}) => {
    return (
        <div className="col">
         <h1>{headings}</h1>
        </div>
    )
        
    
}

export default MovieListHeading